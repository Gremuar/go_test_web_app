#change commiter name: git change-commits GIT_AUTHOR_NAME "старое имя" "новое имя"
#change commiter email: git change-commits GIT_AUTHOR_EMAIL "old@email.com" "new@email.com"
#change with range: git change-commits GIT_AUTHOR_EMAIL "old@email.com" "new@email.com" HEAD~10..HEAD
change-commits = "!f() { VAR=$1; OLD=$2; NEW=$3; shift 3; git filter-branch --env-filter \"if [[ \\\"$`echo $VAR`\\\" = '$OLD' ]]; then export $VAR='$NEW'; fi\" $@; }; f "